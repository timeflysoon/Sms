# 安全政策

## 支持版本

只维护最新的发布版本（见仓库 Releases）。旧版本不再修。

## 报告漏洞

请通过 [GitHub Issues](https://github.com/davidche1116/Sms/issues) 报告，
或直接发邮件给维护者。请附上：影响范围、复现步骤、Android 版本与机型。
涉及短信数据泄露或丢失的，请标注紧急程度，会优先处理。

## 已知事项：仓库内包含签名密钥

`android/key.properties` 与 `android/app/key/sms.keystore` **曾经被提交进
仓库**，这是维护者的明确决策（为了方便在任何机器上直接出可安装的 release
包），但它意味着：

- 任何拿到仓库的人都能产出与本应用**同签名**的 APK；
- 已经从 GitHub Release 安装过本应用的用户，理论上存在被"同签名升级"劫持
  的风险；
- 密钥已在 git 历史中，仅删除文件无法消除，只能轮换密钥。

### 如果你在意这一点

- 只从本仓库 Releases 或你自行构建的包安装；
- 自行构建时删掉 `android/key.properties`，release 会自动回退到调试签名
  （见 `android/app/build.gradle.kts`）；
- 若要彻底解决，需要轮换密钥并在 Release 中公告（会要求用户卸载重装）。

维护者不做密钥轮换属于已知且接受的风险，请不要直接提交"删除密钥"的 PR，
先在 Issue 里讨论。

## 应用的数据边界

- 应用不联网，所有短信数据只留在本机。
- 导出功能会把短信写入应用临时目录并通过系统分享面板交给你选择的目标；
  分享完成后临时 CSV 会被删除。
- 删除短信不可恢复。
